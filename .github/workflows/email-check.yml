name: Email Check Workflow

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 21f3001113@ds.study.iitm.ac.in
        run: echo "Hello, world from GitHub Actions!"
